
chrome.runtime.onInstalled.addListener(() => {
  // Do stuff when extension is installed
})

chrome.runtime.onStartup.addListener(() => {
  // Do stuff when chrome starts
})

// Other code as needed
